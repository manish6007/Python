<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks plain HTTP by default from API 28 onward. The local backend
  runs on http://10.0.2.2:8000 (how an emulator reaches your own machine), so
  cleartext is allowed for local addresses ONLY. Everything else still requires
  HTTPS, which is what a production backend will use.

  Do not add a real domain here.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- The host machine, as seen from the Android emulator. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Typical home/office LAN ranges, for testing on a real phone. -->
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
    </domain-config>
</network-security-config>
